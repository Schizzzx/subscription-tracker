Пока мы не делаем авторизацию, оставим AllowAny.
Позже добавим токены/логин
(backend/subscriptions/views.py)
(serializers.py)

первый юзер:
admin
cocacola

user1
anusanus1

первые две подписки принадлежат первому юзеру
(продублированная подписка на нетфликс)
_

🛠️ Технологии

Python 3

Django

Django REST Framework

SQLite (для разработки)

Virtualenv (venv)

Планируется:

React для web-клиента

React Native / Expo для мобильного приложения

PostgreSQL для продакшн-варианта

🚀 Запуск backend’а (локально)
1. Клонировать репозиторий
git clone https://github.com/Schizzzx/subscription-tracker.git
cd subscription-tracker/backend

2. Создать виртуальное окружение

Windows:
python -m venv venv
venv\Scripts\activate

macOS / Linux:
python3 -m venv venv
source venv/bin/activate

3. Установить зависимости
pip install -r requirements.txt

или, если файла ещё нет:
pip install django djangorestframework psycopg2-binary
pip freeze > requirements.txt

4. Применить миграции
python manage.py migrate

5. Создать суперпользователя (для тестов)
python manage.py createsuperuser

6. Запустить сервер
python manage.py runserver

Backend будет доступен по адресу:

панель DRF: http://127.0.0.1:8000/api/subscriptions/

summary: http://127.0.0.1:8000/api/summary/

админка: http://127.0.0.1:8000/admin/


📡 API эндпоинты (v1)
Subscriptions

Базовый CRUD:
GET     /api/subscriptions/          # список подписок
POST    /api/subscriptions/          # создать подписку
GET     /api/subscriptions/<id>/     # детали
PUT     /api/subscriptions/<id>/     # обновить
DELETE  /api/subscriptions/<id>/     # удалить


Пример объекта:

{
  "id": 1,
  "user": 1,
  "name": "Netflix",
  "price": "12.99",
  "currency": "EUR",
  "billing_period": "monthly",
  "next_payment_date": "2025-12-15",
  "is_active": true
}

Summary
GET /api/summary/

Ответ:
{
  "monthly_total": 25.98,
  "yearly_total": 311.76,
  "count": 2
}

в бэкэнд установили JWT библиотеку
